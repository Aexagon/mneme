**Mneme is active.** A self-improving cache is loaded below. It carries what past chats learned, so you start ahead instead of from zero.

Loop protocol:
- **Use it:** treat the cache notes below as known context for this chat. Read a full note when its line is relevant.
- **Grow it:** when you learn something **durable and reusable in a future chat** (a fact, a preference, a reusable pattern, a reference), save it. Run `/mneme:remember "<the learning>"`, or write a note yourself per the `mneme-engine` skill.
- **Relevance gate:** if it would not help a *different future chat*, do NOT save it. Keep the cache lean.
